import type { CaptionContent } from '@/components/Caption';

export const act2Captions = {
  c1Problem: {
    eyebrow: 'Act II · C1 · Decision framework',
    heading: 'Comparison needs a logic early design can sustain.',
    body: [
      'Placeholder copy. The corridor shell is being built from the metric layout in docs/PLAN.md; this beat is written against the C1 composition once it exists.',
    ],
  } satisfies CaptionContent,
};
